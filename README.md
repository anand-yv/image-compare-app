# image-compare-app
A full-stack web app that compares two images — currently supports face similarity detection via AWS Rekognition, built with React (Vite) and Spring Boot, featuring a clean black-and-white hand-drawn theme.
